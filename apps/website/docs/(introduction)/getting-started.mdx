---
title: Getting Started
description: Oxide is a Vite or Rsbuild plugin that turns one build into a deployable server and optional client.
---

<CardGroup cols={2}>
  <Card title="oxidejs" href="/oxide/overview" icon="box">
    A Vite or Rsbuild plugin. One build writes a server bundle and, if
    you have a page, a client.
  </Card>
  <Card
    title="Configuration"
    href="/oxide/configuration"
    icon="settings"
  >
    Presets, action transport, paths, and deploy keys.
  </Card>
</CardGroup>

## Build an app

- [Oxide overview](/oxide/overview) - what the plugin builds and when to use it
- [Oxide quickstart](/oxide/quickstart) - install the plugin and run a build

## Server features

- [Server entry](/oxide/server-entry) - handle a request, or fall through to static files
- [Server actions](/oxide/server-actions) - `action()`-wrapped RPC, streams, and `useRequest()` / `useCtx()` / `useEnv()` / `useFetchCtx()`
- [Oxide configuration](/oxide/configuration) - presets, paths, and deploy keys
- [Security](/oxide/security) - what oxidejs guards by default
